# Deck

Interactive presenter demo — turn the room into leads. Built on the slide-studio deck template.

- `deck.html` — the deck (entry)
- `index.html` — the phone companion (join)
- `presenter.html` — the live room-map
